# RoomieRules Environment Variables

# JWT Secret - REQUIRED
# Generate a secure random key at least 32 characters long
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-key-minimum-32-characters-long

# Node Environment
# Set to 'production' for production deployments
NODE_ENV=development

# Database Configuration
# SQLite database file path (default: ./roomie-rules.db)
# DATABASE_PATH=./roomie-rules.db

# File Upload Configuration
# Maximum file size for receipt uploads in MB (default: 5MB)
# MAX_FILE_SIZE_MB=5

# Allowed file types for uploads (comma-separated)
# ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp

# App Configuration
# Base URL for the application (used for absolute URLs)
# BASE_URL=http://localhost:3000

# Security Configuration
# Session timeout in hours (default: 24)
# SESSION_TIMEOUT_HOURS=24

# Enable/disable registration for new users
# REGISTRATION_ENABLED=true

# Deployment Configuration (for production)
# VERCEL_URL=your-app.vercel.app
# NEXTAUTH_URL=https://your-app.vercel.app
